# Permalinks
#
# Use of `relative_permalinks` ensures post links from the index work properly.
permalink:           pretty
relative_permalinks: false

plugins:
   - jekyll-paginate

# Setup
title: Brad Keys
highlighter: rouge
paginate: 5
sass:
  style: compressed
